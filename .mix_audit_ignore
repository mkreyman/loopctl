# mix_audit ignore list — advisories with no in-constraint fix available.
# Each entry MUST stay justified; drop it the moment a patched version becomes
# reachable. Consumed by `mix deps.audit --ignore-file` (wired into the
# `precommit` alias in mix.exs AND the Security CI job in .github/workflows/ci.yml).
#
# cowlib 2.18.0 — two advisories, both with NO patched release available: 2.18.0
#   (2026-07-02) is itself the latest cowlib and the flagged version, so there is
#   nothing to bump to. Reviewed 2026-07-22 — recheck when cowlib > 2.18.0 ships.
#   - GHSA-w4f7-4cxr-rv3c / CVE-2026-43966 (MEDIUM): HTTP response splitting via
#     non-VCHAR bytes in cow_http_struct_hd:escape_string/2.
#   - GHSA-g2wm-735q-3f56 / CVE-2026-43969 (LOW): cookie request-header injection
#     in cow_cookie:cookie/1.
#   Exposure is minimal: cowlib is pulled only transitively via
#   telemetry_metrics_prometheus's bundled Plug.Cowboy server, which serves the
#   /metrics scrape endpoint on the INTERNAL :9568 port over Fly's private 6PN
#   network (prod-only, never the public 8080 service). That endpoint sets no
#   cookies and reflects no untrusted request headers, so neither the cookie-
#   injection nor the response-splitting vector is reachable from it. The public
#   API serves on Bandit, not cowboy.
GHSA-w4f7-4cxr-rv3c
GHSA-g2wm-735q-3f56
