{
  "id": "US-39.5",
  "title": "30-day Oban TTL sweep worker for channel_posts",
  "epic": { "id": "epic_39", "name": "Repo Coordination Bus" },
  "priority": "medium",
  "phase": "coordination_bus_v1",
  "story": {
    "as_a": "loopctl operator",
    "i_want_to": "have expired channel posts deleted automatically on a schedule",
    "so_that": "the coordination roll stays small and honors the uniform 30-day retention without manual cleanup"
  },
  "rationale": "Retention is a release gate, not a nicety. A scheduled, idempotent, bounded sweep keeps the table lean and enforces the 30-day window; anything worth keeping longer has already been graduated to Knowledge, so hard-deleting expired rows loses nothing.",
  "background": {
    "reference": "docs/repo-coordination-bus.md §3.4; review D5",
    "includes": "Drop-in template is Loopctl.Workers.SessionMemoryPruneWorker (session_memory_prune_worker.ex): use Oban.Worker, @batch_size 1000, delete_in_batches/2 that loops limit(@batch_size) |> AdminRepo.all() then where(id in ^ids) |> AdminRepo.delete_all(), all-tenants on BYPASSRLS AdminRepo, scheduled {\"*/5 * * * *\"} in oban_config crontab. NOTE: the #411 MemoryGraduationSweepWorker is a scan-and-graduate job, NOT a TTL delete — do not copy it."
  },
  "acceptance_criteria": [
    { "id": "AC-39.5.1", "description": "An Oban worker Loopctl.Workers.ChannelPostSweeper deletes channel_posts WHERE expires_at < now(). Scheduled via the Oban crontab in lib/loopctl/oban_config.ex plugins/0 at a fixed cadence (mirror SessionMemoryPruneWorker's {\"*/5 * * * *\"}).", "status": "pending" },
    { "id": "AC-39.5.2", "description": "The sweep is bounded per run (deletes in batches with a max, logging the count) so a large backlog cannot lock the table; it is safe to run repeatedly (idempotent).", "status": "pending" },
    { "id": "AC-39.5.3", "description": "The sweep runs across all tenants (admin/BYPASSRLS repo, like other maintenance workers) but only ever touches rows past expires_at — it never deletes a live post.", "status": "pending" },
    { "id": "AC-39.5.4", "description": "The worker is registered in the Oban config/cron plugin and appears in Oban observability like the other periodic workers.", "status": "pending" },
    { "id": "AC-39.5.5", "description": "The sweep is ALERTED, not assumed healthy (issue #498). (a) ChannelPostSweeper.perform/1 emits Loopctl.TelemetryEvents.channel_post_swept/0 on EVERY successful run including a zero-delete no-op, so 'nothing to do' is distinguishable from 'never ran'. (b) A raise/DB error is logged at error, emitted as channel_post_sweep_failed/0, and RE-RAISED so Oban still records the failure and retries. (c) The absence-of-success detector Loopctl.Knowledge.IngestionHealth.detect_sweep_stalled/0 flags any ACTIVE tenant still holding channel_posts past expires_at beyond the :sweep_staleness_hours grace window AND beyond the sweep's own drain capacity (the TENANT's own overdue count vs :sweep_drain_rate_per_hour x hours stale, so a healthy-but-backlogged sweep is not paged; :sweep_hard_stale_hours is the ceiling that flags an undrainable backlog regardless; :sweep_scan_limit MUST stay above :sweep_drain_rate_per_hour x :sweep_staleness_hours or that capacity rule is arithmetically unreachable) as a tenant-scoped :sweep_stalled ingestion_anomalies row (reserved source_type IngestionHealth.sweep_source_type/0, CHECK widened by migration 20260722090000_add_sweep_stalled_anomaly_type.exs), raising the operator alert through the existing ScaleAlertDeliveryWorker path exactly once — the alert is ENQUEUED BEFORE the alerted flag is flipped, so a crash or a failed enqueue in the notify gap re-fires next run (at-least-once). (d) Recovery closes the row via IngestionHealth.auto_resolve_recovered_sweep_stalled/1 and re-arms detection, decided on the RAW residue set from detect_sweep_stalled_scan/0 (gated on the tenant still being ACTIVE, closing NOTHING on a truncated scan, and never closing a tenant merely excused as backlogged - so no false 'retention recovered' audit entry is ever written). (e) The detector runs in its own rescue/catch inside IngestionHealthWorker so a saturated AdminRepo pool cannot abort the sibling detectors, and BOTH failure shapes emit Loopctl.TelemetryEvents.sweep_stall_detection_failed/0 so the monitor's own death is observable outside the log stream. (f) The sweep_stalled webhook is its OWN event type coordination.channel_post_sweep_stalled, never knowledge.ingestion_anomaly_detected. DELIBERATE DIVERGENCE from issue #498's wording: (c) is an OUTCOME check (residue still present) rather than the issue's 'no successful sweep observed within a bounded staleness window' heartbeat; the oban_jobs-heartbeat alternative is reasoned and rejected in ChannelPostSweeper's moduledoc. Two gaps are accepted and documented there: a never-scheduled sweeper is undetected while no tenant has posts crossing expires_at + grace, and channel_post_swept/0 (the only positive liveness signal) is EMIT-ONLY in-app - nothing in lib/ attaches to it, per the ScaleMetrics/ScaleAlerts curated-subset convention. There is no heartbeat; do not assume one.", "status": "pending" }
  ],
  "test_cases": [
    { "id": "TC-39.5.1", "name": "sweep deletes expired, keeps live", "type": "integration", "preconditions": ["tenant = fixture(:tenant); project = fixture(:project, %{tenant_id: tenant.id})", "one post with expires_at in the past", "one post with expires_at in the future"], "steps": ["perform ChannelPostSweeper"], "expected_results": ["expired post deleted", "live post remains"] },
    { "id": "TC-39.5.2", "name": "sweep is idempotent (second run no-ops)", "type": "integration", "preconditions": ["no expired rows"], "steps": ["perform the worker twice"], "expected_results": ["{:ok, ...} both times; zero deletions on the second"] },
    { "id": "TC-39.5.3", "name": "batch bound is honored", "type": "integration", "preconditions": ["more expired rows than the batch max"], "steps": ["perform once"], "expected_results": ["at most batch-max deleted in one run; remaining swept next run"] },
    { "id": "TC-39.5.6", "name": "emits sweep-success telemetry carrying the deleted count", "type": "integration", "preconditions": ["two expired posts"], "steps": ["attach a handler for Loopctl.TelemetryEvents.channel_post_swept/0 filtered on the run's limit metadata", "perform ChannelPostSweeper"], "expected_results": ["a swept event with measurements %{deleted: 2}"] },
    { "id": "TC-39.5.7", "name": "emits sweep-success telemetry with deleted: 0 on a no-op run", "type": "integration", "preconditions": ["no expired posts"], "steps": ["perform ChannelPostSweeper"], "expected_results": ["a swept event with measurements %{deleted: 0} — a run that did nothing is still observably a run"] },
    { "id": "TC-39.5.8", "name": "logs at error, emits failure telemetry, and re-raises on a DB failure", "type": "integration", "preconditions": ["the sweep's read is broken at the DB (a session-local temp table shadows channel_posts on the sandbox connection)"], "steps": ["perform ChannelPostSweeper inside capture_log"], "expected_results": ["a Logger.error naming the error_class", "a channel_post_sweep_failed/0 event with %{count: 1} and metadata error_class", "the call RAISES (Oban records the failure and retries)", "no swept event is emitted"] },
    { "id": "TC-39.5.9", "name": "sweep-stall detector flags a tenant with overdue expired posts", "type": "integration", "preconditions": ["two channel_posts expired well beyond :sweep_staleness_hours"], "steps": ["perform IngestionHealthWorker"], "expected_results": ["exactly one :sweep_stalled anomaly under IngestionHealth.sweep_source_type/0 with sample_count = overdue count and last_event_at nil", "exactly one ScaleAlertDeliveryWorker job", "exactly one detected audit entry"] },
    { "id": "TC-39.5.10", "name": "no stall flagged inside the grace window", "type": "integration", "preconditions": ["a post expired only 1h ago (grace is 6h in test config)"], "steps": ["perform IngestionHealthWorker"], "expected_results": ["no anomaly, no operator alert"] },
    { "id": "TC-39.5.11", "name": "a second run does not re-alert; an unalerted row re-fires (at-least-once)", "type": "integration", "preconditions": ["an overdue post; and separately an unresolved sweep_stalled row with alerted: false"], "steps": ["perform IngestionHealthWorker twice / once"], "expected_results": ["exactly one alert across two runs", "the unalerted row re-fires the alert and is then marked alerted"] },
    { "id": "TC-39.5.12", "name": "stall recovery closes the anomaly and re-arms", "type": "integration", "preconditions": ["an open :sweep_stalled anomaly whose overdue rows were then deleted"], "steps": ["perform IngestionHealthWorker"], "expected_results": ["the anomaly is resolved with last_event_at stamped (episode ended, suppression re-armed)"] },
    { "id": "TC-39.5.13", "name": "tenant isolation for the stall detector", "type": "integration", "preconditions": ["tenant A has overdue expired posts; tenant B has only live posts"], "steps": ["perform IngestionHealthWorker"], "expected_results": ["a :sweep_stalled anomaly exists for tenant A only", "tenant B has no anomaly and no detected audit entry"] }
  ],
  "technical_notes": [
    "Worker: Loopctl.Workers.ChannelPostSweeper (use Oban.Worker); schedule in the Oban :crontab.",
    "Delete via the admin repo with a LIMIT/returning-based batch (or delete_all with a subquery LIMIT) to bound each run.",
    "Mirror the existing memory-graduation / retention sweep worker for cadence, logging, and test style."
  ],
  "dependencies": ["US-39.1"],
  "estimated_tokens": 150000
}
